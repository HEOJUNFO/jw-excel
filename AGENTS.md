# jw-excel

수강신청·배정 검토용 엑셀 데이터 작업 폴더.

## Agent skills

실질 작업(기능·리팩터·버그·설계 결정·조사·스펙/티켓·인수인계) 착수 전에 **ask-matt 라우팅**을 하고, 맞는 스킬이 있으면 한 문장으로 알린 뒤 바로 실행한다(추천만 하고 멈추지 않는다). 스킬은 `.claude/skills/`(Claude Code), `.agents/skills/`(Codex — 같은 디렉토리 심링크, git 미추적)에 있고 규칙·라우팅 맵은 `docs/agents/ask-matt-routing.md`(로컬 전용) 참조.

### Issue tracker

이슈·스펙은 이 레포의 `.scratch/<feature>/` 아래 로컬 마크다운 파일로 둔다. `docs/agents/issue-tracker.md`, `triage-labels.md`, `domain.md` 참조.
